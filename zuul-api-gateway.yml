server:
  port: 8080

eureka:
  instance:
    hostname: localhost
  client:
    registerWithEureka: true
    fetchRegistry: false

zuul:
  ignored-services: *
  routes:
    admin-service:
      path: /admin/**
      serviceId: admin-service
      strip-prefix: false